services:
  - type: web
    name: quiz-app
    env: node
    plan: free
    buildCommand: npm run render-build
    startCommand: npm start --prefix server
    envVars:
      - key: NODE_VERSION
        value: 20.18.0
